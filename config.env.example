# Red Hat Console Agent Wrapper Configuration

# Required: JWT token for Red Hat Console API authentication
# This should be the same askRH token used elsewhere
ARH_JWT_TOKEN=your-jwt-token-here

# Optional: Bearer token required for client authentication
# Change this to a secure token in production
AGENT_BEARER_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxx
